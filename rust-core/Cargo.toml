[package]
name = "hybrid-brain-core"
version = "0.1.0"
edition = "2021"
description = "Hot-loop neuronal simulation core — PyO3-ready, ARM64-friendly"

# ADR-002 constraint: from Week 12, every change must remain compatible with
# cargo build --target aarch64-unknown-linux-gnu.
[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# None for now — add pyo3 only during the transition phase (Weeks 12–14)
